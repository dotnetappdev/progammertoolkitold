<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ProgrammersToolKit.Views.NotepadView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <ScrollViewer>
        <StackPanel Margin="32" Spacing="12">
            <TextBlock Text="Featured Notepad" FontWeight="Bold" FontSize="20"/>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button x:Name="NewNoteBtn" Content="New Note" Width="100"/>
                <Button x:Name="SaveNoteBtn" Content="Save Note" Width="100"/>
                <Button x:Name="DeleteNoteBtn" Content="Delete Note" Width="100"/>
                <Button x:Name="RunMacroBtn" Content="Run Macro" Width="100"/>
            </StackPanel>
            <ComboBox x:Name="NotesCombo" Width="300" Margin="0,8,0,0" HorizontalAlignment="Left"/>
            
            <!-- Using TextBox as replacement for AvaloniaEdit - in production, consider WebView2 with Monaco Editor -->
            <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="4">
                <TextBox x:Name="NoteEditor" 
                         AcceptsReturn="True" 
                         FontFamily="Consolas" 
                         Height="220" 
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         PlaceholderText="Start typing your note here..."/>
            </Border>
            
            <TextBox x:Name="MacroBox" 
                     PlaceholderText="Macro (C# code, e.g. replace, format, etc)" 
                     AcceptsReturn="True" 
                     Height="60" 
                     FontFamily="Consolas"/>
            <TextBlock x:Name="StatusBlock" FontWeight="SemiBold" Margin="0,12,0,0"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>